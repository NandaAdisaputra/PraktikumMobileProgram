<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nandaadisaputra.praktikum1mobileprogram">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".activity.crud.CrudActivity"></activity>
        <activity android:name=".activity.GoogleMapActivity" />
        <activity android:name=".activity.IfElseActivity" />
        <activity android:name=".activity.FormPenjualanActivity" />
        <activity android:name=".activity.BangunDatarActivity" />
        <activity android:name=".activity.KalkulatorActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>